CREATE TABLE 'properties' (key BLOB, value BLOB);

CREATE TABLE 'ranks' (id INTEGER, level INTEGER, name TEXT, props TEXT);

CREATE TABLE 'user_ranks' (userid INTEGER, rank INTEGER);

CREATE TABLE 'admin_ranks' (id INTEGER, level INTEGER);

